```sh
sudo ip addr add 192.168.21.10/24 dev pnet1
sudo ip link set pnet1 up

sudo ip addr flush dev pnet2
sudo ip addr add 10.1.1.20/24 dev pnet2
sudo ip link set pnet2 up
```

# 启动网卡

```sh
sudo ip link set pnet1 up
sudo dhclient eth1
```



# 或者

```sh
sudo ip addr add 10.1.1.20/24 dev pnet1
sudo ip link set eth1 up
```
