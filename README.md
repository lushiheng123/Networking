<h1 align="center">学一些产品<h1>

```sh
git init
git checkout -b products
git add .
git commit -m "first commit"
git remote add origin git@github.com:lushiheng123/Networking.git
git branch
git push -u origin products
```

```sh
git init
git remote add origin git@github.com:lushiheng123/Networking.git
git remote -v
git fetch origin
git branch -r
git pull origin products
```
