git init .

git checkout -b RoCE

git add .和 git status 常规进行

git commit -m "first commit"   设置任务

git remote add origin git@github.com:lushiheng123/Networking.git 设置远程连接

git push -u origin RoCE 选择用什么分支上传